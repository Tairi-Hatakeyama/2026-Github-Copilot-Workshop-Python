{
  "name": "pomodoro-timer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:py": "python -m pytest tests/ -v",
    "test:js": "vitest run",
    "test": "npm run test:js && npm run test:py"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
