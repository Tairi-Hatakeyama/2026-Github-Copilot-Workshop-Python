# Pomodoro Timer Webアプリケーション アーキテクチャ案

## 1. 目的

本ドキュメントは、このプロジェクトで実装するPomodoro Timer Webアプリケーションのアーキテクチャ方針を整理したものである。対象アプリはFlaskをバックエンドに採用し、HTML/CSS/JavaScriptでフロントエンドを構築する。

今回の設計では、以下を重視する。

- シンプルに実装を始められること
- タイマー処理が時間ずれに強いこと
- ユニットテストを書きやすいこと
- 将来的な機能追加に耐えられること
- モバイルを含むレスポンシブUIを実現できること

## 2. 必要機能

本アプリで最低限必要な機能は以下のとおり。

- 25分の作業タイマー
- 5分の休憩タイマー
- タイマーの開始・停止・リセット
- 進捗表示と統計機能
- ブラウザ通知とサウンド通知
- レスポンシブなWeb UI

## 3. アーキテクチャ概要

アプリケーションは、以下の4層で分離する。

1. フロントエンド層
2. API層
3. ドメイン層
4. 永続化層

### 3.1 フロントエンド層

責務:

- タイマー表示の更新
- ボタン操作の受付
- 進捗表示の描画
- 通知とサウンド再生の制御
- ブラウザ内状態の保持と復元

実装方針:

- HTMLで構造を定義する
- CSSでレスポンシブUIを構築する
- JavaScriptで状態管理とタイマー制御を行う
- タイマーはブラウザ側で動作させ、秒数の減算ではなく終了予定時刻との差分で残り時間を計算する

### 3.2 API層

責務:

- Flaskで画面を配信する
- 設定情報の取得・更新APIを提供する
- 統計情報の取得・保存APIを提供する

実装方針:

- JSON APIとして提供する
- ビジネスルールはAPI層に直接書かず、ドメイン層を呼び出す
- App Factoryパターンを採用し、テスト時に依存を差し替えられる構成にする

### 3.3 ドメイン層

責務:

- ポモドーロの状態遷移を管理する
- 作業モードと休憩モードの切り替えルールを持つ
- 進捗率、残り時間、完了イベントの判定を行う

実装方針:

- Flask、DOM、LocalStorage、通知APIには依存しない純粋ロジックとして実装する
- 入力を現在状態とイベント、出力を次状態と副作用指示に限定する
- ユニットテストの対象となる中核ロジックとして設計する

### 3.4 永続化層

責務:

- 設定の保存
- 統計情報の保存
- 必要に応じたセッション復元支援

実装方針:

- 初期段階ではLocalStorageを主要な保存先とする
- 将来的な拡張を見据え、統計や設定の保存先はSQLiteへ移行可能な形で抽象化する

## 4. 推奨ディレクトリ構成

実装ファイルはPomodoroアプリの配置ルールに従い、1.pomodoro配下にまとめる。

```text
1.pomodoro/
  app.py
  templates/
    index.html
  static/
    css/
      style.css
    js/
      app.js
      timer_store.js
      timer_controller.js
      notification_service.js
  domain/
    timer.py
    models.py
    events.py
  services/
    stats_service.py
    config_service.py
  tests/
    test_timer.py
    test_stats_service.py
    test_api.py
```

ルートには、この設計ドキュメントとしてarchitecture.mdを配置する。

## 5. 状態管理の設計

### 5.1 モード

- work
- break

### 5.2 実行状態

- idle
- running
- paused

### 5.3 主なイベント

- start
- pause
- resume
- reset
- complete

### 5.4 代表的な状態遷移

- idle + start -> running
- running + pause -> paused
- paused + resume -> running
- running + reset -> idle
- running + complete(work) -> break の running もしくは idle
- running + complete(break) -> work の running もしくは idle

自動で次のモードへ進めるか、完了後に待機状態へ戻すかはUI要件に応じて選べるが、実装初期はルールを固定してシンプルに保つのが望ましい。

## 6. タイマー制御方針

タイマーの精度を保つため、残り秒を毎秒減算する方式は採用しない。

採用方針:

- 開始時に終了予定時刻を保持する
- 表示更新時に現在時刻との差分から残り時間を再計算する
- 一時停止時は残り秒を保存する
- 再開時は保存済み残り秒から新しい終了予定時刻を再計算する

この方式により、ブラウザの描画遅延やタブ非アクティブ時のsetIntervalのずれに強くなる。

## 7. UI設計方針

### 7.1 主要UIコンポーネント

- タイマー表示エリア
- 現在モード表示
- 開始・停止・リセットボタン
- 進捗表示バーまたは円形インジケーター
- 統計表示パネル
- 通知許可状態の表示

### 7.2 レスポンシブ方針

- モバイル優先で設計する
- 主要ブレークポイントは 480px、768px、1024px を想定する
- ボタンや主要操作領域はタップしやすいサイズを確保する
- 情報量の多い統計パネルは狭い画面では縦積みにする

### 7.3 フロントエンド責務の分離

- app.js: 画面初期化とイベント接続
- timer_store.js: 現在状態の保持と更新
- timer_controller.js: タイマー進行と状態遷移制御
- notification_service.js: 通知とサウンド再生

## 8. 通知設計

### 8.1 ブラウザ通知

- Notification APIを利用する
- 初回操作時に通知権限を要求する
- タイマー完了時に現在モードに応じた通知を表示する

### 8.2 サウンド通知

- ユーザー操作後に再生可能なAudio APIを利用する
- 作業完了と休憩完了で同一もしくは異なる通知音を使う
- 再生失敗時はUI上で完了を明確に示し、通知の欠落を補う

## 9. 統計機能の設計

初期段階では日次統計を対象とする。

### 9.1 保存対象

- 当日の完了ポモドーロ数
- 当日の集中時間合計
- 休憩完了数

### 9.2 データモデル例

Config:

- work_minutes
- break_minutes

SessionState:

- mode
- status
- end_at
- remaining_seconds
- started_at

DailyStats:

- date
- completed_work_sessions
- completed_break_sessions
- focused_minutes

## 10. API設計案

最小構成では以下のAPIを用意する。

### 10.1 設定取得

- GET /api/config

レスポンス例:

```json
{
  "work_minutes": 25,
  "break_minutes": 5
}
```

### 10.2 設定更新

- PUT /api/config

リクエスト例:

```json
{
  "work_minutes": 25,
  "break_minutes": 5
}
```

### 10.3 当日統計取得

- GET /api/stats/today

レスポンス例:

```json
{
  "date": "2026-04-17",
  "completed_work_sessions": 3,
  "completed_break_sessions": 2,
  "focused_minutes": 75
}
```

### 10.4 イベント記録

- POST /api/stats/events

リクエスト例:

```json
{
  "event_type": "work_completed",
  "completed_at": "2026-04-17T10:30:00"
}
```

## 11. テスト容易性を高める設計上の追加方針

今回のアプリでは、UIよりも状態遷移と時間制御の正しさが重要である。そのため、ユニットテストのしやすさを前提に以下を採用する。

### 11.1 ドメインロジックを純粋関数または純粋オブジェクトとして実装する

- 現在状態とイベントから次状態を返す
- I/Oや通知、保存処理を直接呼び出さない

### 11.2 Clock依存を注入する

- 現在時刻取得を直接書かない
- SystemClockとFakeClockを差し替え可能にする

### 11.3 Port/Adapterで副作用を分離する

- 通知
- サウンド
- ストレージ
- 統計保存

これらは抽象インターフェース経由で呼び出し、テストではFake実装に差し替える。

### 11.4 FlaskアプリをDI可能にする

- create_app(config, services) 形式で初期化する
- テスト時にFakeServiceを渡せるようにする

### 11.5 バリデーションを独立させる

- APIハンドラ内に入力検証を埋め込まない
- 専用関数または専用モジュールで検証し、個別にテストする

### 11.6 契約テストを追加する

- フロントエンドが依存するJSONレスポンス形式を固定する
- API変更時の破壊的変更を検知しやすくする

## 12. テスト戦略

### 12.1 ドメイン単体テスト

対象:

- start, pause, resume, reset の各遷移
- work と break の切り替え
- 完了時のイベント発行
- 経過時間に対する残り時間計算

### 12.2 APIテスト

対象:

- 設定取得・更新
- 不正入力時のバリデーションエラー
- 統計取得・イベント記録

### 12.3 フロントエンドロジックテスト

対象:

- 画面表示更新
- 進捗率表示
- ボタン状態の切り替え
- 通知呼び出し条件

## 13. 実装ステップ案

実装は以下の順序で進めるのが望ましい。

1. ドメイン層の状態遷移ロジックを実装する
2. ドメイン層のユニットテストを追加する
3. FlaskのApp FactoryとAPIを作成する
4. フロントエンドの状態ストアとタイマー制御を実装する
5. 通知とサウンド機能を追加する
6. 統計表示と永続化を追加する
7. レスポンシブUIを仕上げる

## 14. 完了条件

以下を満たした時点で、初期リリース相当の完成とみなす。

- 25分作業と5分休憩の切り替えが正しく動作する
- 開始・停止・リセットが全状態で破綻しない
- 進捗表示が残り時間と一致する
- 統計情報が記録・表示される
- タイマー完了時にブラウザ通知とサウンド通知が発火する
- モバイル表示でも操作性と視認性が維持される

## 15. 補足

本アーキテクチャは、まず最小構成で安定して動くことを優先した案である。将来的には以下の拡張が可能である。

- 長休憩の導入
- ユーザーごとの設定保存
- SQLiteによる統計永続化
- PWA化によるオフライン対応
- テーマ切り替えやUIカスタマイズ